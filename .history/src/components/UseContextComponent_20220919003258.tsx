import { useState } from "react";
import UserContext from "./store";
function UseContextComponent(){
    const [user,setUser]=usestte
    return(
    <UserContext.Provider >

    </UserContext.Provider>
    );
}