import { useReducer } from "react";
function countReducer(state:typeof in){

}
function UseReducerComponent(){
const [state,dispatch]=useReducer(counterReducer,initialState)
}
export default UseReducerComponent;