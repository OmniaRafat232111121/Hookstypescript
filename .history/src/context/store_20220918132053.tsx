import { createContext } from "react";
const initialState={
    first:'omnia',
    last:'rafat'
};
const context=<tycreateContext of initialState></tycreateContext(initialState);
export default context;