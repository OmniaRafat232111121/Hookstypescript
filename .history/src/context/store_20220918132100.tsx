import { createContext } from "react";
const initialState={
    first:'omnia',
    last:'rafat'
};
const context=<tycreateContext of initialState></tcreateContext(initialState);
export default context;