import { createContext } from "react";
const initialState={
    first:'omnia',
    last:'rafat'
};
const context=<typecreateContext of initialState></typcreateContext(initialState);
export default context;