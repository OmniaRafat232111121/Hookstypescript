import { createContext } from "react";
const initialState={
    first:'omnia',
    last:'rafat'
};
export typeof UserSate=typeo
const context= createContext<typeof initialState> (initialState);
export default context;