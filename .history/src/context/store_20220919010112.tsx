import { createContext } from "react";
const initialState={
    first:'omnia',
    last:'rafat'
};
export typeof UserSate=typeof initialSt
const context= createContext<typeof initialState> (initialState);
export default context;